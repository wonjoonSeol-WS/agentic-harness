# AGENTS.md

<!--
  Table of contents for AI agents working on this project.
  Keep this file concise (~100 lines max). Agents load it on every turn,
  so brevity is critical for effective context utilization.

  This file POINTS to deeper docs -- it does not contain everything.
  Auto-generated by Agentic Harness. Customize each section for your project.
-->

## Project Overview

- **Name**: my-project
- **Purpose**: _Describe the project's purpose in 1-2 sentences._
- **Tech Stack**: _e.g., Kotlin 2.x, Spring Boot 3.x, PostgreSQL, Redis_
- **Repository Structure**: _e.g., multi-module Gradle / monorepo / single package_

## Key References

<!-- Pointers to deeper documentation -- agents navigate here for details -->

| Document | Path | What It Contains |
|----------|------|-----------------|
| Architecture | `ARCHITECTURE.md` | Domain map, layer rules, data flow |
| Design Docs | `docs/design-docs/` | Why decisions were made |
| Exec Plans | `docs/exec-plans/` | Step-by-step implementation plans |
| Product Specs | `docs/product-specs/` | What to build |

## Module Structure

<!-- For monorepos: list each module with a one-line description -->
<!-- Each module can have its own AGENTS.md with module-specific conventions -->

| Module | Path | Description |
|--------|------|-------------|
<!-- | auth-service | services/auth/ | JWT-based authentication and authorization | -->
<!-- | payment-service | services/payment/ | Payment processing via Stripe/PayPal | -->
<!-- | common | core/common/ | Shared utilities, DTOs, and base classes | -->

## Conventions

<!-- Coding conventions that agents MUST follow. Be specific and prescriptive. -->

- **Language style**: _e.g., prefer immutable data classes, no wildcard imports_
- **Naming**: _e.g., services end in `Service`, repositories end in `Repository`_
- **File organization**: _e.g., one class per file, test mirrors source structure_
- **Formatting**: _e.g., run `ktlintFormat` / `prettier` before committing_
- **Commits**: _e.g., conventional commits, reference issue number_
- **Imports**: Use explicit imports only -- no wildcard imports
- **Layer rules**: See `ARCHITECTURE.md` for import direction rules

## Golden Principles

<!--
  Mechanical rules that are always enforced. These are non-negotiable.
  Add rules here as they are discovered through code review and incidents.
-->

- _e.g., Entity fields must be updated through domain methods, not directly_
- _e.g., All new API endpoints must include Swagger annotations_
- _e.g., Never commit secrets or credentials_
- _e.g., Database migrations must be backwards-compatible_

## Quality Grades

<!--
  Track code quality per domain/module. Update after reviews and audits.
  Grades: A (excellent), B (good), C (needs improvement), D (poor)
-->

| Domain | Grade | Known Gaps | Last Reviewed |
|--------|-------|------------|--------------|
<!-- | auth | B | Missing rate limiting on login endpoint | 2026-01 | -->
<!-- | payment | A | - | 2026-02 | -->
<!-- | shared | C | Too many utility classes, needs cleanup | 2025-12 | -->

## Quality Checks

<!--
  Commands that run automatically after the agent writes code.
  These are configured in harness.yml under execution.post_commands.
-->

- _e.g., `./gradlew ktlintFormat` -- format Kotlin code_
- _e.g., `./gradlew compileKotlin` -- verify compilation_
- _e.g., `./gradlew test` -- run all tests_

## Testing

- **Framework**: _e.g., Kotest + MockK, Jest, pytest_
- **Run all tests**: _e.g., `./gradlew test`, `npm test`, `pytest`_
- **Test patterns**:
  - Unit tests live alongside source in `src/test/`
  - Use descriptive test names: `should [expected] when [condition]`
  - Mock external dependencies, never call real APIs in tests

## Common Pitfalls

<!-- Known gotchas that agents frequently trip on. Add to this list over time. -->

- _e.g., The `config/` directory is auto-generated -- do not edit manually_
- _e.g., Redis cache keys follow the pattern `{service}:{entity}:{id}`_

## Lessons Learned

<!--
  Auto-updated by the Agentic Harness feedback loop.
  When recurring mistakes are detected (threshold: 3 occurrences),
  they are appended here automatically.
-->

See [.harness/lessons-learned.jsonl](.harness/lessons-learned.jsonl) for the full history.
